# Post-Response: Prettier Check

After every response where you wrote or modified code:

1. Run `bun run prettier:check` from the repo root
2. If there are errors, run `bun run prettier` to fix them
3. If you cannot fix an issue, tell the user what failed and why
4. If check passes, do not mention it — stay silent
